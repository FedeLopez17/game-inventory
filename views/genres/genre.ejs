<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/style.css">
    <title><%= genre.name %></title>
  </head>
  <body>
    <%- include("../partials/header", { activeEntity: "genres" }) %>

    <p><%= genre.name %></p>
    <p><%= genre.description %></p>
  </body>
</html>
